# AGENTS.md Compatibility Shim

`CLAUDE.md` is the canonical repo instruction file for coding agents in this project. Follow it first.

For frontend design direction, also follow `.impeccable.md`.

Do not duplicate long-lived rules here; update `CLAUDE.md` and `.impeccable.md` instead so Codex, Cursor, and Claude Code stay aligned.
